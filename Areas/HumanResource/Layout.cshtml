@{
    Layout = null;
    var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Human Resource Dashboard</title>

    <!-- Fonts & Icons -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">

    <!-- Stylesheets -->
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/hr-layout.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/notifications.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/hr-dashboard.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/messenger.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/employee-layout.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/project_lifecycle.styles.css" asp-append-version="true" />
    @await RenderSectionAsync("Styles", required: false)
</head>
<body class="hr-body">

    <!-- Mobile overlay -->
    <div class="hr-sidebar-overlay" id="sidebarOverlay"></div>

    <div class="hr-layout">

        <!-- ========== SIDEBAR ========== -->
        <aside class="hr-sidebar" id="hrSidebar">

            <!-- Brand -->
            <div class="hr-sidebar-header">
                <a href="@Url.Action("Index", "Dashboard", new { area = "HumanResource" })" class="hr-sidebar-brand">
                    <div class="hr-brand-icon">
                        <img src="~/css/Images/LOGO (final).png" asp-append-version="true" alt="Project Lifecycle Logo" class="brand-logo hr-brand-logo" />
                    </div>
                    <span class="emp-brand-text">
                        Project <br />
                        Lifecycle
                    </span>
                </a>
                <button class="hr-sidebar-toggle" id="sidebarToggle" title="Toggle Sidebar">
                    <i class="fas fa-bars fa-sm"></i>
                </button>
            </div>

            <!-- Navigation -->
            <nav class="hr-sidebar-nav">
                <span class="hr-nav-section-title">Menu</span>
                <ul class="hr-nav-list">
                    <li class="hr-nav-item">
                        <a href="@Url.Action("Index", "Dashboard", new { area = "HumanResource" })"
                           class="hr-nav-link @(currentController == "Dashboard" ? "active" : "")"
                           data-tooltip="Dashboard">
                            <i class="fas fa-th-large"></i>
                            <span class="hr-nav-text">Dashboard</span>
                        </a>
                    </li>
                    <li class="hr-nav-item">
                        <a href="@Url.Action("Index", "User", new { area = "HumanResource" })"
                           class="hr-nav-link @(currentController == "User" ? "active" : "")"
                           data-tooltip="User Management">
                            <i class="fas fa-users"></i>
                            <span class="hr-nav-text">User Management</span>
                        </a>
                    </li>

                    <li class="hr-nav-item">
                        <a href="#" class="hr-nav-link hr-collapsible-toggle @(currentController == "Department" || currentController == "Position" ? "expanded" : "")"
                           data-target="managementSubmenuHR"
                           data-tooltip="Management">
                            <i class="fas fa-tasks"></i>
                            <span class="hr-nav-text">Management</span>
                            <i class="fas fa-chevron-down hr-nav-arrow"></i>
                        </a>
                        <ul class="hr-nav-submenu @(currentController == "Department" || currentController == "Position" ? "expanded" : "")" id="managementSubmenuHR">
                            <li class="hr-nav-subitem">
                                <a href="@Url.Action("Index", "Department", new { area = "HumanResource" })"
                                   class="hr-nav-sublink @(currentController == "Department" ? "active" : "")">
                                    <i class="fas fa-building"></i> Departments
                                </a>
                            </li>
                            <li class="hr-nav-subitem">
                                <a href="@Url.Action("Index", "Position", new { area = "HumanResource" })"
                                   class="hr-nav-sublink @(currentController == "Position" ? "active" : "")">
                                    <i class="fas fa-briefcase"></i> Positions
                                </a>
                            </li>
                        </ul>
                    </li>

                    <li class="hr-nav-item">
                        <a href="@Url.Action("Index", "Messages", new { area = "HumanResource" })"
                           class="hr-nav-link @(currentController == "Messages" ? "active" : "")"
                           data-tooltip="Messages">
                            <i class="fas fa-comment-dots"></i>
                            <span class="hr-nav-text">Messages</span>
                            <span class="msg-nav-badge" style="display:none"></span>
                        </a>
                    </li>

                    <li class="hr-nav-item">
                        <a href="@Url.Action("Index", "AuditLog", new { area = "HumanResource" })"
                           class="hr-nav-link @(currentController == "AuditLog" ? "active" : "")"
                           data-tooltip="Audit Logs">
                            <i class="fas fa-clipboard-list"></i>
                            <span class="hr-nav-text">Audit Logs</span>
                        </a>
                    </li>
                </ul>
            </nav>

        </aside>

        <!-- ========== MAIN ========== -->
        <div class="hr-main">

            <!-- Header -->
            <header class="hr-header" id="hrHeader">
                <div class="hr-header-left">
                    <button class="hr-mobile-toggle" id="mobileSidebarToggle" aria-label="Open menu">
                        <i class="fas fa-bars"></i>
                    </button>
                    <nav class="hr-breadcrumb" aria-label="Breadcrumb">
                        <a href="@Url.Action("Index", "Dashboard", new { area = "HumanResource" })" class="hr-breadcrumb-home">
                            <i class="fas fa-home"></i>
                        </a>
                        <span class="hr-breadcrumb-sep"><i class="fas fa-chevron-right"></i></span>
                        <span class="hr-breadcrumb-current">@(ViewData["Title"] ?? "Dashboard")</span>
                    </nav>
                </div>

                <div class="hr-header-right">
                    <div class="notif-wrapper">
                        <button class="hr-header-btn notif-bell-btn" id="notifBellBtn" title="Notifications" aria-expanded="false">
                            <i class="fas fa-bell"></i>
                            <span class="notif-badge" id="notifBadge">0</span>
                            <span class="hr-notif-dot" id="notifDot" style="display:none"></span>
                        </button>
                        <div class="notif-dropdown" id="notifDropdown">
                            <div class="notif-dropdown-header">
                                <h6><i class="fas fa-bell"></i> Notifications</h6>
                                <button class="notif-mark-all" id="notifMarkAllRead" style="display:none">Mark all read</button>
                            </div>
                            <div class="notif-list" id="notifList"></div>
                            <div class="notif-empty" id="notifEmpty">
                                <i class="fas fa-bell-slash"></i>
                                <span>No notifications yet</span>
                            </div>
                        </div>
                    </div>

                    <div class="hr-header-divider"></div>

                    <div class="hr-header-profile" id="userDropdownToggle" style="cursor:pointer;position:relative;">
                        <div class="hr-header-avatar" id="headerUserAvatar">HR</div>
                        <div class="hr-header-user-info">
                            <div class="hr-header-greeting" id="headerGreeting">Welcome back</div>
                            <div class="hr-header-name" id="headerUserName">Human Resource</div>
                        </div>
                        <i class="fas fa-chevron-down" style="font-size:.7rem;color:#64748b;margin-left:.25rem"></i>
                        <div class="hr-user-dropdown" id="userDropdown">
                            <a href="@Url.Action("Index", "Profile", new { area = "HumanResource" })" class="hr-user-dropdown-item">
                                <i class="fas fa-user"></i>
                                <span>Profile</span>
                            </a>
                            <div class="hr-user-dropdown-divider"></div>
                            <button type="button" class="hr-user-dropdown-item hr-user-dropdown-logout" id="logoutBtn">
                                <i class="fas fa-arrow-right-from-bracket"></i>
                                <span>Logout</span>
                            </button>
                        </div>
                    </div>
                </div>
            </header>

            <!-- Page Content -->
            <main class="hr-content">
                @RenderBody()
            </main>
        </div>

    </div>

    <!-- Hidden logout form -->
    <form action="/Identity/Account/Logout" method="post" id="logoutForm" style="display:none;">
        @Html.AntiForgeryToken()
        <input type="hidden" name="returnUrl" value="/" />
    </form>

    <!-- Logout confirmation modal -->

    <style>
    /* User dropdown */
    .hr-user-dropdown { position:absolute; top:calc(100% + 4px); right:0; background:#fff; border-radius:8px; box-shadow:0 2px 12px rgba(0,0,0,.10); min-width:150px; padding:.3rem 0; display:none; z-index:1500; }
    .hr-user-dropdown.active { display:block; }
    .hr-user-dropdown-item { display:flex; align-items:center; gap:.45rem; padding:.35rem .75rem; font-size:.8rem; color:#334155; text-decoration:none; cursor:pointer; border:none; background:transparent; width:100%; text-align:left; }
    .hr-user-dropdown-item:hover { background:#f1f5f9; color:#0f172a; }
    .hr-user-dropdown-item i { width:15px; text-align:center; font-size:.78rem; }
    .hr-user-dropdown-divider { height:1px; background:#e2e8f0; margin:.15rem 0; }
    .hr-user-dropdown-logout { color:#ef4444; }
    .hr-user-dropdown-logout:hover { background:#fef2f2; color:#dc2626; }
    .hr-modal-backdrop { position: fixed; inset: 0; background: rgba(3,7,18,0.6); display: none; align-items: center; justify-content: center; z-index: 2000; }
    .hr-modal-backdrop.active { display: flex; }
    .hr-modal { background: #fff; border-radius: 12px; width: 94%; max-width: 420px; padding: 1.25rem; box-shadow: 0 10px 30px rgba(2,6,23,0.35); }
    .hr-modal h4 { margin: 0 0 .5rem; font-size: 1.05rem; color: #0f172a; }
    .hr-modal p { margin: 0 0 1rem; color: #64748b; font-size: .92rem; }
    .hr-modal .hr-modal-actions { display:flex; gap:.5rem; justify-content:flex-end; }
    .hr-modal .hr-btn { border-radius: 8px; padding: .5rem .85rem; font-weight:600; }
    .hr-modal .hr-btn-cancel { background: transparent; border:1px solid #e6eef6; color:#475569; }
    .hr-modal .hr-btn-confirm { background: linear-gradient(90deg,#006994,#4CA3DD); color:#fff; border:none; }
    </style>

    <div class="hr-modal-backdrop" id="logoutModal" role="dialog" aria-modal="true" aria-hidden="true">
        <div class="hr-modal" role="document">
            <h4>Sign out</h4>
            <p>Are you sure you want to sign out of your account?</p>
            <div class="hr-modal-actions">
                <button type="button" class="hr-btn hr-btn-cancel" id="cancelLogoutBtn">Cancel</button>
                <button type="button" class="hr-btn hr-btn-confirm" id="confirmLogoutBtn">Sign out</button>
            </div>
        </div>
    </div>

    <!-- Scripts -->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    <script src="~/js/notifications.js" asp-append-version="true"></script>
    <script src="~/js/header-user.js" asp-append-version="true"></script>
    <script src="~/lib/microsoft-signalr/dist/browser/signalr.min.js"></script>
    <script src="~/js/messenger.js" asp-append-version="true"></script>

    <script>
    (function () {
        const sidebar  = document.getElementById('hrSidebar');
        const toggle   = document.getElementById('sidebarToggle');
        const mobile   = document.getElementById('mobileSidebarToggle');
        const overlay  = document.getElementById('sidebarOverlay');
        const header   = document.getElementById('hrHeader');
        const greeting = document.getElementById('headerGreeting');

        /* --- Time-based greeting --- */
        const hour = new Date().getHours();
        if (greeting) {
            greeting.textContent = hour < 12 ? 'Good morning' : hour < 18 ? 'Good afternoon' : 'Good evening';
        }

        /* --- Sidebar collapse toggle (desktop) --- */
        toggle?.addEventListener('click', function () {
            const icon = this.querySelector('i');
            const collapsed = sidebar.classList.toggle('collapsed');
            icon.className = collapsed ? 'fas fa-chevron-right fa-sm' : 'fas fa-bars fa-sm';
            this.setAttribute('title', collapsed ? 'Expand Sidebar' : 'Collapse Sidebar');
            localStorage.setItem('hrSidebarCollapsed', collapsed);
        });

        /* --- Restore collapsed state --- */
        if (localStorage.getItem('hrSidebarCollapsed') === 'true') {
            sidebar?.classList.add('collapsed');
            const icon = toggle?.querySelector('i');
            if (icon) {
                icon.className = 'fas fa-chevron-right fa-sm';
                toggle.setAttribute('title', 'Expand Sidebar');
            }
        }

        /* --- Mobile sidebar --- */
        function openMobile()  { sidebar?.classList.add('mobile-open');    overlay?.classList.add('active'); }
        function closeMobile() { sidebar?.classList.remove('mobile-open'); overlay?.classList.remove('active'); }

        mobile?.addEventListener('click', function () {
            sidebar?.classList.contains('mobile-open') ? closeMobile() : openMobile();
        });
        overlay?.addEventListener('click', closeMobile);

        document.addEventListener('click', function (e) {
            if (window.innerWidth <= 768 &&
                sidebar?.classList.contains('mobile-open') &&
                !sidebar.contains(e.target) &&
                !mobile?.contains(e.target)) {
                closeMobile();
            }
        });

        /* --- Sticky header shadow on scroll --- */
        window.addEventListener('scroll', function () {
            header?.classList.toggle('scrolled', window.scrollY > 8);
        }, { passive: true });

        /* --- Notifications --- */
        if (typeof initNotifications === 'function') initNotifications();

        /* --- Collapsible submenu handling --- */
        document.querySelectorAll('.hr-collapsible-toggle').forEach(function (btn) {
            btn.addEventListener('click', function (e) {
                e.preventDefault();
                const targetId = this.getAttribute('data-target');
                const submenu = document.getElementById(targetId);
                const isExpanded = this.classList.contains('expanded');

                if (submenu) {
                    if (isExpanded) {
                        this.classList.remove('expanded');
                        submenu.classList.remove('expanded');
                    } else {
                        this.classList.add('expanded');
                        submenu.classList.add('expanded');
                    }
                }

                // Close other submenus
                document.querySelectorAll('.hr-collapsible-toggle').forEach(function (other) {
                    if (other !== btn) {
                        const otherId = other.getAttribute('data-target');
                        const otherSub = document.getElementById(otherId);
                        if (otherSub) {
                            other.classList.remove('expanded');
                            otherSub.classList.remove('expanded');
                        }
                    }
                });
            });
        });

        /* --- User dropdown --- */
        const userDropdownToggle = document.getElementById('userDropdownToggle');
        const userDropdown = document.getElementById('userDropdown');
        userDropdownToggle?.addEventListener('click', function (e) {
            e.stopPropagation();
            userDropdown?.classList.toggle('active');
        });
        document.addEventListener('click', function (e) {
            if (!userDropdownToggle?.contains(e.target)) {
                userDropdown?.classList.remove('active');
            }
        });

        /* --- Logout confirmation modal --- */
        const logoutForm = document.getElementById('logoutForm');
        const logoutBtn = document.getElementById('logoutBtn');
        const logoutModal = document.getElementById('logoutModal');
        const confirmLogoutBtn = document.getElementById('confirmLogoutBtn');
        const cancelLogoutBtn = document.getElementById('cancelLogoutBtn');

        function showLogoutModal() {
            if (!logoutModal) return;
            logoutModal.classList.add('active');
            logoutModal.setAttribute('aria-hidden', 'false');
        }
        function hideLogoutModal() {
            if (!logoutModal) return;
            logoutModal.classList.remove('active');
            logoutModal.setAttribute('aria-hidden', 'true');
        }

        logoutBtn?.addEventListener('click', function (e) {
            e.preventDefault();
            showLogoutModal();
        });

        cancelLogoutBtn?.addEventListener('click', function () { hideLogoutModal(); });

        // Close modal when clicking backdrop
        logoutModal?.addEventListener('click', function (e) {
            if (e.target === logoutModal) hideLogoutModal();
        });

        confirmLogoutBtn?.addEventListener('click', function () {
            if (logoutForm) logoutForm.submit();
        });
    })();
    </script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>

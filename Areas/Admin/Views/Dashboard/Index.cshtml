@{
    ViewData["Title"] = "Super Admin Dashboard";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <h1 class="text-primary mt-4">Super Admin Dashboard</h1>
            <p class="lead">Welcome to the verifiable Super Admin area.</p>
            <hr />
            <div class="card">
                <div class="card-header bg-primary text-white">
                    Admin Actions
                </div>
                <div class="card-body">
                    <p class="card-text">You have full access to manage the system.</p>
                    <a href="#" class="btn btn-outline-primary">Manage Users</a>
                    <a href="#" class="btn btn-outline-secondary">System Settings</a>
                </div>
            </div>
        </div>
    </div>
</div>
